# Школьная столовая - система управления питанием

Веб-приложение для автоматизации процесса заказа питания в школьной столовой. Система позволяет ученикам просматривать меню, формировать заказы и оплачивать их онлайн, а администраторам - управлять меню, пользователями и отслеживать заказы.

## 🚀 Быстрый старт

### Предварительные требования

Для работы с проектом необходимо установленное программное обеспечение следующих версий:

- <b>Node.js</b> версии 16 или выше - среда выполнения JavaScript

- <b>npm</b> версии 8 или выше - менеджер пакетов, устанавливается вместе с Node.js
   
- <b>PocketBase</b> версии 0.22 или выше - легковесный бэкенд-сервер
  
- <b>Git</b> для клонирования репозитория и управления версиями
  
Основные библиотеки фронтенда:

- <b>React 19.2.0</b> - библиотека для построения пользовательских интерфейсов

- <b>React Router DOM 7.12.0</b> - маршрутизация между страницами

- <b>Bootstrap 5.3.8</b> - CSS-фреймворк для стилизации компонентов

- <b>PocketBase 0.26.5</b> - клиент для работы с API PocketBase

- <b>Vite 7.3.1</b> - сборщик проекта с поддержкой горячей перезагрузки

## Полная структура базы данных PocketBase

Система использует <b> PocketBase</b> как единый бэкенд, объединяющий сервер базы данных, API и аутентификацию. Все данные хранятся в SQLite, встроенной в PocketBase, что исключает необходимость установки и настройки отдельных СУБД.

### Коллекция users - пользователи системы

Является центральной коллекцией, содержащей информацию о всех учениках школы. Каждая запись включает в себя:
- Поле "<b>student_id</b>", содержащее уникальный идентификационный номер
- Поля "<b>first_name</b>" и "<b>last_name</b>" хранят имя и фамилию пользователя
- Поле "<b>class</b>" - класс обучения в формате 5а, 6б, 7в и так далее до 11 класса

### Коллекция dishes - блюда школьного меню

Содержит полную карточку каждого блюда, доступного для заказа. 
 - Поле "<b>name</b>" - название блюда, отображаемое в меню и документах
 - Поле "<b>description</b>" - подробное текстовое описание, включающее особенности приготовления и подачи
 - Поле "<b>ingredients</b>" - полный состав блюда, перечень ингредиентов, важный для учеников с пищевыми аллергиями или особыми диетическими потребностями
 - Поле "<b>price</b>" - цена в рублях, хранится как числовое значение с поддержкой копеек
 - Поле "<b>weight</b>" указывает вес порции в граммах, отображается в формате 200 г, 100 г, 28 г
 - Поле "<b>image</b>" предназначено для загрузки файлов непосредственно в PocketBase, при этом система автоматически создает несколько версий изображения для разных разрешений
 - Поле "<b>image_url</b>" позволяет указать ссылку на внешнее изображение, что удобно при использовании готовых фотографий с CDN-серверов
 - Поле "<b>is_available</b>" логического типа управляет доступностью блюда. При значении <b>false</b> блюдо не отображается в меню и недоступно для заказа, но сохраняется в базе данных
 - Поле "<b>category</b>" ограничено шестью значениями:
   - "<b>Супы</b>"
   - "<b>Салаты</b>"
   - "<b>Основные блюда</b>"
   - "<b>Гарниры</b>"
   - "<b>Десерты</b>"
   - "<b>Напитки</b>"
   
Категоризация обеспечивает удобную навигацию по меню и фильтрацию.

### Коллекция orders - заказы

Фиксирует каждую транзакцию оформления заказа. 
 - Поле "<b>user_id</b>" является внешним ключом, связывающим заказ с конкретным пользователем из коллекции <b>users</b>
 - Поле "<b>items</b>" хранит позиции заказа в формате JSON. Каждый элемент массива содержит идентификатор блюда, его название на момент заказа, количество порций, цену за единицу и общую стоимость позиции
 - Поле "<b>total_amount</b>" автоматически рассчитывается как сумма всех заказов в <b>items</b> и дублирует общую стоимость для быстрого доступа без парсинга JSON
 - Поле "<b>order_date</b>" автоматически устанавливается при создании заказа, поле delivery_date заполняется в момент выдачи.
 - Поля "<b>payment_method</b>" и "<b>payment_status</b>" независимо отслеживают способ оплаты и ее статус.
 - Поле "<b>status</b>" отражает статус заказа в снастоящий момент. Принимает значения:
   - "<b>Обработка</b>" в момент оформления
   - "<b>Оплачено</b>" после подтверждения оплаты
   - "<b>Готовится</b>" когда заказ передан на кухню, ready после приготовления
   - "<b>Готово</b>" при выдаче ученику 
   - "<b>Отменено</b>" в случае отмены

Каждое изменение статуса фиксируется с временной меткой.

### Коллекция carts - корзины пользователей

Выполняет функцию временного хранилища перед оформлением заказа. 
 - Каждая корзина жестко привязана к пользователю через поле "<b>user_id</b>" с ограничением уникальности - у одного пользователя может быть только одна активная корзина.
 - Поле "<b>items</b>" имеет ту же структуру JSON, что и в заказах, но может часто изменяться.
 - Поля "<b>total_items</b>" и "<b>total_amount</b>" автоматически пересчитываются при любом изменении состава корзины, обеспечивая актуальную информацию для отображения в интерфейсе.
 - Поле "<b>expires_at</b>" устанавливается равным created + 24 часа. По истечении этого срока корзина автоматически удаляется системой очистки, что предотвращает накопление устаревших данных.

### Коллекция payments - платежи и финансовые операции

Обеспечивает полную финансовую отчетность и аудит всех денежных движений. 
- Поле "<b>user_id</b>" связывает платеж с пользователем
- Поле "<b>order_id</b>" опционально связывает платеж с конкретным заказом
- Поле amount хранит сумму операции, положительное значение для пополнений, отрицательное для списаний
- Поле "<b>method</b>" принимает значения
  - "<b>cash</b>"
  - "<b>card</b>" или "<b>balance</b>", указывая способ проведения платежа.
- Поле "<b>status</b>" отражает состояние платежа:
  - "<b>pending</b>" для операций в процессе обработки,
  - "<b>completed</b>" для успешно завершенных
  - "<b>failed</b>" при ошибках проведения.
- Поле "<b>transaction_id</b>" сохраняет идентификатор внешней транзакции при интеграции с платежными системами.
- Поле "<b>description </b>" содержит человекочитаемое описание операции, например, Пополнение баланса через кассу, Оплата заказа №123, Возврат средств за отмененный заказ. Временная метка "<b>created</b>" фиксирует момент совершения операции.

### Установка и настройка окружения

#### Подготовка рабочего окружения

  1. Необходимо убедиться в наличии установленного <b>Node.js</b> версии 16 или выше. Откройте терминал и выполните команду  ``` node -v ```
      - Если Node.js не установлен, скачайте установщик с официального сайта по ссылке ``` https://nodejs.org/en ```

  2. Аналогично проверьте наличие <b>Git</b> командой ```git --version```
      - <b>Git</b> необходим для клонирования репозитория и управления версиями кода. Скачать Git можно с официального сайта ```git-scm.com```


#### Клонирование репозитория

Выполните в терминале команду:
```
git clone https://github.com/glitch-tiger/stolovaya_serv.git
```
#### Установка зависимостей фронтенда
После завершения клонирования перейдите в директорию проекта:
```
cd stolovaya_serv
```
В корневой директории проекта выполните:
```
npm install
```
Эта команда прочитает файл <b>package.json</b> и установит все необходимые зависимости, включая <b>React</b>, <b>React Router</b>,<b> Bootstrap</b>, <b>PocketBase</b> и <b>Vite</b> с плагинами. Процесс установки может занять несколько минут в зависимости от скорости интернет-соединения.

#### Настройка PocketBase

Перейдите в директорию <b>pocketbase</b> в структуре проекта. Здесь находится исполняемый файл <b>pocketbase.exe</b> для Windows или <b>pocketbase</b> для Linux и macOS. Запустите сервер командой:

```
./pocketbase serve
```

При первом запуске PocketBase создаст директорию <b>pb_data</b> для хранения базы данных <b>SQLite</b> и директорию <b>pb_migrations</b> для миграций. Сервер будет доступен по адресу ``` http://127.0.0.1:8090 ```

#### Создание коллекций базы данных

Откройте браузер и перейдите по адресу ``` http://127.0.0.1:8090/_/ ```. Это интерфейс администратора PocketBase. Нажмите "<b>Создать первую коллекцию</b>" и последовательно создайте коллекции 
 - "<b>users</b>"
 - "<b>dishes</b>"
 - "<b>orders</b>"
 - "<b>carts</b>"
 - "<b>payments</b>"
с полями, описанными в разделе структуры базы данных.

#### Настройка подключения к PocketBase

Откройте файл <b>src/pb.js</b>. По умолчанию в нем настроено подключение к локальному серверу ```http://127.0.0.1:8090 ```

Для работы с удаленным сервером измените URL на ``` https://pb.dev.zavidovo.school```. Убедитесь, что на удаленном сервере созданы все необходимые коллекции и настроены соответствующие права доступа.

#### Запуск приложения в режиме разработки

После завершения настройки проекта, в терминале пропишите:

``` npm run start ```

Vite запустит сервер разработки, и приложение станет доступно по адресу удалённого сервера. При каждом сохранении файлов страница будет автоматически перезагружаться с внесенными изменениями.

## 📁 Структура проекта

```
stolovaya_serv/
├── public/                 # Статические файлы
├── src/
│   ├── components/         # React компоненты
│   ├── pages/             # Страницы приложения
│   │   ├── Users.js       # Управление учениками
│   │   ├── MenuManagement.js # Управление меню
│   │   ├── LoginPage.js   # Страница входа
│   │   ├── UserProfile.js # Профиль пользователя
│   │   └── ...
│   ├── pocketbase.js      # Конфигурация PocketBase
│   └── App.jsx           # Основной компонент приложения
├── package.json           # Зависимости проекта
└── README.md             # Документация
```

## 🛠 Быстрые команды

### `npm run start`

Запускает приложение в режиме разработки.

Страница автоматически перезагружается при внесении изменений в код.

### `npm run serve`

Предпросмотр собранного проекта.

### `npm run build`

Собирает приложение для production в папку `build`.\
Оптимизирует сборку для лучшей производительности.

Сборка минифицирована, имена файлов содержат хеши.\
Приложение готово к деплою!

### `npm run deploy`

Деплой на GitHub Pages.
